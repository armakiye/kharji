name: sms_expense
description: ردیاب هزینه از اس‌ام‌اس بانکی

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  telephony: ^0.2.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.1.0
  intl: ^0.19.0
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
